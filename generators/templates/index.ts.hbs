import {{ pascalCase name }} from './{{ pascalCase name }}'

customElements.define('{{> tagName }}', {{ pascalCase name }})

declare global {
  interface HTMLElementTagNameMap {
    '{{> tagName }}': {{ pascalCase name }}
  }
}
